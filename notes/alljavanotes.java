package notes;

public class alljavanotes {
    public static void main(String[] args) {
        // input
        //  Scanner sc = new Scanner(System.in);
        //  int ans = sc.nextInt();

        // conversion
        //  String s = Integer.toString(ans);  int to string
        //  int num = Character.getNumericValue(s.charAt(i));   character to int
        //  String s = String.valueOf(N);    string to int 

        // methods
        //  int farthest = Math.max(farthest, i + arr[i]);   max
        //  int n = Math.pow(temp,s.length());   power
        // int n = Math.min(temp,s.length());   min

        // read number by single digit
        //  int temp = num%10;
        //  num/=10;

        // reverse the digit
        //  int digit = temp%10;
        //  rev = rev*10+digit;
        //  temp/=10;
        
        // long datatype
        //  Long A = 5L;
        //  Long B = 5L;

        // max and min 
        //  Integer.MIN_VALUE
        //  Integer.MAX_VALUE
        
        //  Convert j to the corresponding letter (A is 65 in ASCII)
    }
}
